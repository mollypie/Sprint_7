# Sprint_7

```
1. test_create_courier_with_first_name() - проверяет, что можно создать курьера, передав обязательные параметры (логин и пароль) и имя курьера
```

```
2. test_create_courier_without_first_name() - проверяет, что можно создать курьера, передав только обязательные параметры (логин и пароль)
```

```
3. test_create_two_identical_couriers() - проверяет, что нельзя создать 2 одинаковых курьера
```

```
4. test_create_with_exists_login() - проверяет, что нельзя создать курьера с существующим логином
```

```
5. test_create_courier_without_password() - проверяет, что нельзя создать курьера, не передав обязательный параметр (пароль)
```

```
6. test_create_courier_without_login() - проверяет, что нельзя создать курьера, не передав обязательный параметр (логин)
```

```
7. test_login_courier_with_valid_credentials() - проверяет, что курьер может войти в существующий аккаунт используя логин и пароль
```

```
8. test_login_courier_with_invalid_password() - проверяет, что курьер не может войти в существующий аккаунт используя некорректный пароль
```

```
9. test_login_courier_with_invalid_login() - проверяет, что курьер не может войти в существующий аккаунт используя некорректный логин 
```

```
10. test_login_courier_with_non_existent_password() - проверяет, что курьер не может войти в существующий аккаунт используя несуществующий пароль
```

```
11. test_login_courier_with_non_existent_login() - проверяет, что курьер не может войти в существующий аккаунт используя несуществующий логин
```

```
12. test_create_order() - проверяет, можно создать заказ с одним, несколькими или без цветов самоката
```

```
13. test_get_list_orders_with_courier_id() - проверяет, что курьер может получить список своих заказов
```

```
14. test_get_list_orders_with_limit_and_page() - проверяет, что можно получить список 10ти доступных для взятия курьером заказов
```

```
15. test_get_order_with_non_exists_courier_id() - проверяет, что нельзя получить список заказов несуществующего курьера
```
